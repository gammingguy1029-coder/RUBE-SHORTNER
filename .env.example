NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
ADMIN_PASSWORD=
TOKEN_SECRET=
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Leave this UNSET. Ads now render in-page via app/components/AdUnit.tsx, so
# nothing reads it. It belongs to the abandoned separate-origin iframe approach:
# Adsterra binds each ad unit key to the site it was created under, so keys
# served from a second origin return 403. Kept only so the old AdFrame component
# still type-checks.
NEXT_PUBLIC_ADS_ORIGIN=
