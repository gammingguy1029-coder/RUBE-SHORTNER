NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
ADMIN_PASSWORD=
TOKEN_SECRET=
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# DEPRECATED — delete from Vercel. All ads now render in-page from main domain
# via app/components/AdUnit.tsx + SocialBar + Popunder. The separate-origin
# iframe path (lib/ads.ts + AdFrame) is removed. Leave unset or remove.
# NEXT_PUBLIC_ADS_ORIGIN=
